package com.example.mobileapplication.ui.theme

import androidx.compose.ui.graphics.Color

// ── Accent / brand ────────────────────────────────────────────────────────────
val CyanBlue       = Color(0xFF2298BC)
val YellowGreen    = Color(0xFFD5DF4A)
val CyanBlueLight  = Color(0xFFB3E0F2)   // light container tint

// ── Dark structural palette ───────────────────────────────────────────────────
val DarkBackground = Color(0xFF0A0A0A)   // main screen background
val DarkSurface    = Color(0xFF141414)   // cards / dialogs
val DarkNavbar     = Color(0xFF1A1A1A)   // top bar / bottom nav
val DarkDivider    = Color(0xFF2C2C2C)   // subtle dividers

// ── On-colors (text / icons on dark surfaces) ─────────────────────────────────
val OnDark         = Color(0xFFE8E8E8)   // primary text
val OnDarkMuted    = Color(0xFF888888)   // secondary / hint text
